PORT=3000
LOG_LEVEL=info

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=belong
DB_PASSWORD=belong_dev
DB_DATABASE=fan_rewards

JWT_ACCESS_SECRET=change-me-to-a-secure-random-string
JWT_REFRESH_SECRET=change-me-to-another-secure-random-string

# Optional: comma-separated browser origins to allow (CORS). Leave unset to
# reflect any origin in local dev; set explicitly in production, e.g.:
# CORS_ORIGINS=https://app.getbelong.app,https://admin.getbelong.app
# CORS_ORIGINS=
